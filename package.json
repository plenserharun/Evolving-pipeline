{
  "name": "evolving-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "Static Evolving Pipeline quotation app with Supabase-backed storage.",
  "scripts": {
    "build": "node -e \"console.log('Static app: no build required')\"",
    "deploy:cloudflare": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.99.0"
  }
}
